bootstrap_prompt.txt

You are starting fresh on gosper-gcf. Do not assume any context beyond what I paste next.

State capsule:
- Repo is https://github.com/egp/gosper-gcf and is source of truth
- main is currently green for normal suite: go test -count=1 ./core ./named ./trig
- pending-only failures remain:
  - core sqrt(2) prefix test red
  - core square(sqrt2) timeout
  - named SinDegrees: 30° wrong CF digit; 90°, -30°, 69° timeout
- previous branch cleanup is complete; work should start from current main on a new feature branch
- recent merged work stabilized pending SinDegrees panic handling in core range/chooser

Work style:
- use only the files/errors I provide next as source of truth
- do not assume older branch state or hidden context
- prefer minimal, surgical changes
- keep normal suite green at all times
- run full pending suite often enough to catch regressions
- prioritize mathematical correctness and technical rigor over speed
- Cache WIP files, do not cache anything else. Use repo for all else.
- discard obsolete WIP after a push, discard previous log files, etc.