bootstrap_prompt.txt

You are continuing gosper-gcf, a Go library for exact generalized continued-fraction arithmetic in Gosper/HAKMEM style.

State capsule:
- GitHub main is authoritative for committed work
- during an R/G cycle, only WIP files provided in-chat are cached until I say “committed”
- all non-pending tests are green
- parked pending reds: sqrt(2) known-prefix signoff, and Square(Sqrt2()) deferred under current DLFT infinite-algebraic limitations
- E() and Pi() are now robust infinite procedural named sources
- GCF.Take(n) and GCF.Rational(n) are implemented and green
- docs/spec.txt and docs/roadmap.txt were updated to current truth
- next active feature is a new sibling package trig/, with Sin first and Tanh second

Working style:
- do not assume any context beyond what I paste next
- work only from current repo files, code, tests, compiler errors, and explicit instructions I provide
- mathematical correctness first
- no floats anywhere
- keep files compiling at all times
- add public stubs first, then full pending BB suite, then strict WB R/G cycles
- remove skip logic only when tests are truly green
- provide complete replacement files when changing code
- all .go files must have first and last line filename/version comments


The repo at https://github.com/egp/gosper-gcf is the authority for what has been committed and pushed.

Read the following files from the docs/ directory in the repo to get caught up.
checkpoint.txt
open_questions.txt
current_task.txt
roadmap.txt
spec.txt

THen let's dive into the trig library /trig