gosper-gcf roadmap purpose

This document is the execution roadmap for gosper-gcf.
It emphasizes sequencing, test strategy, and current development slices.

----------------------------------------------------------------------
1. current status
----------------------------------------------------------------------

Completed enough:
- foundational exact types
- PQStream finite and procedural sources
- GCF shell and exact terminal state
- BLFT engine, live unary and binary evaluation
- public BLFT ops: Reciprocal, Add, Sub, Mul, Div
- DLFT engine for finite/rational unary work
- public Square
- public Sqrt, implemented enough for all easy signoff cases
- GCF.Take(n)
- GCF.Rational(n)
- named Sqrt2 procedural source
- named E infinite procedural source
- named Pi infinite procedural Gauss source
- RCFStream interface
- PQStreamFromRCF
- PQStreamFromRational
- named.FromRational convenience wrapper
- exact range BB checks for named E and Pi
- stronger Pi validation against OEIS A001203

Deferred but tracked:
- DLFT infinite algebraic-source cases under the current PQStream + Range contract, especially Square(Sqrt2())

Current milestone:
- keep the one remaining sqrt signoff red parked
- design trig as a sibling package
- implement Sin first, then Tanh

Critical path to target formula:
1. finish parked sqrt(2) signoff
2. Sin
3. Tanh
4. final target expression integration

----------------------------------------------------------------------
2. completed phases
----------------------------------------------------------------------

Phase 0 - repository skeleton
Done enough.

Phase 1 - foundational domain types
Done enough:
- Status
- PQTerm
- RCFTerm
- Rational
- RangeKind
- Endpoint
- Range

Phase 2 - input source-stream edge
Done enough:
- PQStream contract
- finite validating source
- procedural source
- Range() on live sources
- EOF handling

Phase 3 - client-facing regular-output edge
Done enough:
- GCF shell
- NextRCF()
- Range()
- exact terminal state
- optional config plumbing

Phase 4 - BLFT center part 1
Done enough:
- BLFT state
- normalization helpers
- ingest formulas
- collapse formulas
- terminal exact-state collapse
- engine seam groundwork

Phase 5 - BLFT center part 2
Done enough for current scope:
- corner-range evaluation
- widest-range ingest choice
- CanEmitRCFTerm
- emission formula
- live unary and binary evaluator paths

Cmp is still deferred until needed.

Phase 6 - unary ops and DLFT introduction
Done enough for current scope:
- DLFTCoefficients
- DLFT state
- DLFT ingest / emit / collapse
- finite/rational candidate-range work
- shared unary-engine integration
- Square
- Reciprocal

Phase 7 - richer operations and named
Done enough for current scope:
- Add
- Sub
- Mul
- Div
- Sqrt public path implemented enough for easy signoff cases
- Take(n)
- Rational(n)
- named Sqrt2
- named E infinite procedural source
- named Pi infinite procedural Gauss source
- FromRational wrapper
- exact range BB checks for named E and Pi
- stronger Pi prefix and convergent validation

----------------------------------------------------------------------
3. parked phase - sqrt
----------------------------------------------------------------------

Goal:
- finish full Sqrt(X) signoff as a live Gosper/Ouroboros feedback stream

Hard requirements:
- no preread to EOF
- no finite-only shortcut path
- no source-specific knowledge
- only PQ terms, Range(), and ordinary collapse
- emitted terms feed back as approximation terms
- ingest only when needed

Current permanent substrate already available:
- RCFStream
- PQStreamFromRCF
- PQStreamFromRational
- feedback/replay support for emitted approximation terms
- existing public ops needed for Newton refinement: Add, Div, Mul, Reciprocal, Square

Pending BB signoff list for sqrt:
- TestBB_GCF_SqrtOfTwoMatchesKnownPrefix

Status of pending sqrt tests:
- intentionally skipped by default
- runnable with RUN_PENDING_TESTS=1
- the remaining parked pending red is sqrt(2) known-prefix behavior

Related deferred BB gap outside sqrt proper:
- TestBB_SquareOfSqrt2IsExactlyTwo remains intentionally pending under the current DLFT infinite algebraic-source limitation

----------------------------------------------------------------------
4. next active phase - trig framework
----------------------------------------------------------------------

Goal:
- create a reusable trig framework in a sibling package
- implement Sin first
- implement Tanh second
- leave the rest of the trig / hyperbolic family to later follow-on work

Planned package direction:
- core remains algebraic and foundational
- named remains constants and convenience wrappers
- trig becomes the sibling package for transcendental kernels and public trig builders

Shared framework plan:
- replay/fork support for one lazily emitted kernel stream reused by multiple algebraic consumers
- one shared odd-denominator Lambert-family kernel framework
- circular half-angle kernel for tan(x/2)
- hyperbolic half-angle kernel for tanh(x/2)
- thin algebraic composers layered on top of those kernels

Public implementation order:
1. trig.Sin(x) in radians
2. named.SinDegrees(x) as a wrapper using Pi()/180
3. trig.Tanh(x)
4. later Cos, Tan, and the remaining circular / hyperbolic family

Target cycle budget:
- aim to finish Sin and Tanh together in fewer than seven R/G cycles

----------------------------------------------------------------------
5. immediate implementation order for trig
----------------------------------------------------------------------

Step 1
Add the trig package skeleton and the full pending BB signoff suite for Sin and Tanh.

Step 2
Add the replay/fork substrate and the first WB tests for it.

Step 3
Add the shared Lambert-family half-angle kernel substrate and its WB tests.

Step 4
Implement Sin in radians on top of the circular half-angle kernel.

Step 5
Implement named.SinDegrees as a Pi()/180 wrapper around trig.Sin.

Step 6
Implement Tanh on top of the hyperbolic half-angle kernel.

Acceptance rule:
- As each pending BB test turns green, remove its skip gate.

----------------------------------------------------------------------
6. later work
----------------------------------------------------------------------

Still later after Sin and Tanh:
- finish the parked sqrt(2) signoff case if still deferred
- implement the rest of the trig / hyperbolic family
- target formula integration
- revisit DLFT infinite algebraic-source gap
- implement Cmp if and when needed
- add property-based tests where they materially strengthen confidence

----------------------------------------------------------------------
7. testing and workflow rules
----------------------------------------------------------------------

Feature-start workflow:
1. add public stubs
2. add full BB signoff list as pending
3. activate tests as implementation turns them green
4. add WB tests per R/G cycle
5. remove skip logic from green tests

All procedural named sources should provide exact current ranges where feasible.
All BB tests of named sources should verify Range() at each step.
Procedural named-source validation should reach at least 50 terms when practical.
Use stronger published gold standards, such as OEIS, when available and appropriate.

Use RUN_PENDING_TESTS=1 to force pending tests on and expose the full TODO surface for a feature.

----------------------------------------------------------------------
8. current top priority
----------------------------------------------------------------------

Keep the one parked sqrt(2) pending red explicit, but begin the trig framework and implement Sin first.