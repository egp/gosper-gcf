Project: gosper-gcf, a Go library for Gosper/HAKMEM-style continued-fraction arithmetic using internal GCF (p,q) streams plus interval/range reasoning, with companion packages named and trig; north star is docs/gosper_cf_requirements_spec.md.
Current milestone: Phase 1.5 complete enough to commit; tree is green after CurrentInterval surface migration and stabilization.
Working components: ./tools/check.sh passes; go test ./core ./named ./trig passes; CurrentInterval() exists across stream interfaces and implementations; core/named/trig compile and test green.
Working behavior: independent-of-X/Y collapse remaps are stabilized to preserve existing BB behavior; projectX/projectY BB tests are green again.
Working behavior: direct unary outside-range Range() errors remain explicit; transitional PQStreamFromRCF adapter may advance past an unsupported current interval if later tail interval becomes usable.
Working behavior: sqrt production code is consolidated into core/sqrt.go; core/sqrt_controller.go, core/sqrt_cycle4.go, and core/sqrt_cycle5.go are obsolete placeholders and can be deleted if still empty in repo.
Known divergence from spec: newGCF2WithResolvedConfig still performs constructor-time shortcutting for independent-of-X/Y cases; collapse is not yet fully lazy per spec.
Known divergence from spec: internal RCF replay/adapters still exist in places, especially outside core’s ideal internal (p,q)/interval-only design.
Known divergence from spec: checked-constructor migration is incomplete; NewRational compatibility seam still exists.
Known future work: add at least one testify-based test, but only after module metadata / go.sum issues are intentionally handled.
Next immediate task: commit all Phase 1.5 work with a broad commit message, then start next phase focused on spec alignment rather than more interface churn.
Next phase target: remove remaining trig-specific design leakage from core and/or make constructor collapse fully lazy, but discuss any spec deviations explicitly before changing behavior.
Constraint: treat docs/gosper_cf_requirements_spec.md as authoritative north star; discuss any intentional divergence.
Constraint: prioritize core first, then named, then trig.
Constraint: no production panics; prefer explicit errors.
Constraint: internal representation should stay in (p,q) / interval space as much as possible; only client-facing boundary should emit RCF.
Constraint: do not remove helpers unless there is an explicit unused-helper error from the user/tooling.
Constraint: new tests added from now on should use testify unless there is a compelling reason not to.
Constraint: bump version comments on any file you replace.