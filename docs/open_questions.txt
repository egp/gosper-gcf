open_questions.txt

- Exact public/package split: trig.Sin/trig.Tanh in trig/, and degrees wrappers only in named/?
- Exact shape of the shared replay/fork substrate for kernel outputs: generic observed-prefix fan-out helper or trig-specific helper?
- Exact internal API for the shared Lambert-family engine: one parameterized kernel for circular vs hyperbolic, or two thin wrappers over one common evaluator?
- For Sin v1, how much circular argument reduction/quadrant logic is in scope, versus principal-domain-only behavior plus degrees wrappers?
- Which exact BB signoff cases for Sin and Tanh should be included in cycle 1 versus deferred to later hardening/property tests?