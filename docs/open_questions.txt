open_questions.txt

- Is the remaining SinDegrees problem primarily in trig/lambert.go, trig/replay_rcf.go, or the interaction of replayed kernel output with sinFromTanHalf?
- Does the circular Lambert root currently provide enough progress/accuracy for exact-angle cases like 30° and 90°, or is there a deeper root-generation limitation?
- Are the 90° / -30° / 69° timeouts caused by lack of progress in replay/caching, or by a semantically wrong but non-panicking kernel path?
- After SinDegrees is fixed, do sqrt(2) and square(sqrt2) still fail independently, or are any of those failures sharing a common progress/collapse issue?