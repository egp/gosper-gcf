current_task.txt

Start from current main on a new feature branch for SinDegrees semantic fixes.
First task: diagnose and fix pending SinDegrees failures without regressing the normal suite.

Current highest-priority failure:
- RUN_PENDING_TESTS=1 go test -count=1 ./named -run 'TestBB_Named_SinDegrees_ThirtyIsExactlyOneHalf'
- observed failure: term 2 = 1, want 2

Acceptance criteria:
- normal suite remains green: go test -count=1 ./core ./named ./trig
- pending SinDegrees no longer has panics
- ideally all pending named SinDegrees tests pass; at minimum the next step should be evidence-based and reduce failures without regressions
- keep sqrt(2) and square(sqrt2) parked unless directly needed

Recommended first move:
- rerun full pending suite from current main
- inspect trig-layer files (especially circular Lambert / replay / progress path) before touching core again