current_task.txt

Start the trig framework in a new sibling package trig/.
Do the feature-start step first: add the full pending BB signoff suite for Sin and Tanh, with compile-safe wrong stubs so everything builds.
Use the approved shared design:
- replay/fork substrate for lazily emitted kernel prefixes
- shared odd-denominator Lambert-family kernel
- circular half-angle kernel tan(x/2)
- hyperbolic half-angle kernel tanh(x/2)
- trig.Sin(x) in radians
- named.SinDegrees(x) as Pi()/180 wrapper
- trig.Tanh(x) after Sin
Acceptance criteria for the first slice:
- code compiles
- full pending BB suite for Sin and Tanh exists
- first WB slice is ready to begin
- parked sqrt(2) pending red remains untouched
Single highest-priority design question:
- exact internal framework and package/file structure for trig replay + Lambert half-angle kernels shared by both Sin and Tanh